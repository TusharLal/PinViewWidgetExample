<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <stroke
        android:width="@dimen/pin_stroke_width"
        android:color="@color/pin_background_drawable_color" />

    <solid android:color="@color/four_pin_default_bg" />

    <size
        android:width="@dimen/pin_width"
        android:height="@dimen/pin_height" />
</shape>